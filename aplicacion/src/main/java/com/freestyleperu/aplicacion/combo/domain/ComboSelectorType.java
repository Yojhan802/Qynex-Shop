package com.freestyleperu.aplicacion.combo.domain;

public enum ComboSelectorType {
    /** La línea exige un producto específico. */
    PRODUCT,
    /** La línea exige N unidades de cualquier producto de una categoría (y opcionalmente una marca). */
    CATEGORY
}
